# Phase 1: Data Collection & Integration

In this stage, we collect data from different raw sources and perform an initial integration. 
The goal is to link the **Prices dataset** with the **Market locations lookup table** to ensure each record has geographical context.

**Current Task:**
* Load `prices.csv` and `markets_lookup.csv`.

  
* Verify data structure.
* Merge datasets using `market_id`.

  
